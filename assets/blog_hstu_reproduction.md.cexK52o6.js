import{_ as i,c as s,o as a,aj as t}from"./chunks/framework.CIIM_L32.js";const k=JSON.parse(`{"title":"HSTU Model Reproduction Guide","description":"Reproduction notes for Meta's HSTU model in torch-rechub, including architecture design and implementation details","frontmatter":{"title":"HSTU Model Reproduction Guide","description":"Reproduction notes for Meta's HSTU model in torch-rechub, including architecture design and implementation details"},"headers":[],"relativePath":"blog/hstu_reproduction.md","filePath":"en/blog/hstu_reproduction.md"}`),n={name:"blog/hstu_reproduction.md"};function o(l,e,d,r,h,p){return a(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="reproducing-the-hstu-model-in-torch-rechub" tabindex="-1">Reproducing the HSTU Model in torch-rechub <a class="header-anchor" href="#reproducing-the-hstu-model-in-torch-rechub" aria-label="Permalink to “Reproducing the HSTU Model in torch-rechub”">​</a></h1><p>This document describes the current implementation status of HSTU (Hierarchical Sequential Transduction Units) in <code>torch-rechub</code>. The core layer follows the main path of Eqs. 2-4 in the paper and Meta&#39;s reference implementation: the joint UVQK projection is passed through SiLU, <code>rab^{p,t}</code> is added to the attention scores, the output path retains only <code>f_2(Norm(AV) * U)</code>, and inter-layer residual connections are placed outside <code>HSTUBlock</code> layers.</p><hr><h2 id="_1-module-structure" tabindex="-1">1. Module Structure <a class="header-anchor" href="#_1-module-structure" aria-label="Permalink to “1. Module Structure”">​</a></h2><p>The main HSTU-related modules are:</p><ul><li><strong>Main model</strong>: <code>torch_rechub/models/generative/hstu.py</code><ul><li><code>HSTUModel</code>: item token embeddings, position/time input embeddings, <code>HSTUBlock</code>, and output projection.</li></ul></li><li><strong>Core layer and block</strong>: <code>torch_rechub/basic/layers.py</code><ul><li><code>HSTULayer</code>: a single sequential transduction unit implementing HSTU Eqs. 2-4.</li><li><code>HSTUBlock</code>: stacks layers using <code>x = x + HSTULayer(x)</code>.</li></ul></li><li><strong>HSTU/HLLM utilities</strong>: <code>torch_rechub/utils/hstu_utils.py</code><ul><li><code>RelativeBucketedTimeAndPositionBias</code>: the <code>rab^{p,t}</code> attention-score bias used by HSTU.</li><li><code>RelPosBias</code>: a legacy relative-position bias retained for HLLM and compatibility experiments; the current <code>HSTUModel</code> does not use it.</li><li><code>VocabMask</code>: masks PAD or invalid items during inference/ranking.</li></ul></li><li><strong>Data and training</strong>: <ul><li><code>torch_rechub/utils/data.py</code>: <code>SeqDataset</code> and <code>SequenceDataGenerator</code>.</li><li><code>torch_rechub/trainers/seq_trainer.py</code>: <code>SeqTrainer</code>.</li><li><code>examples/generative/run_hstu_movielens.py</code> and <code>examples/generative/run_hstu_amazon_books.py</code>: training and ranking-evaluation examples.</li></ul></li></ul><h3 id="minimal-reproduction-commands" tabindex="-1">Minimal Reproduction Commands <a class="header-anchor" href="#minimal-reproduction-commands" aria-label="Permalink to “Minimal Reproduction Commands”">​</a></h3><p>Run all commands below from the repository root. The <code>processed/</code> directory is generated by preprocessing; it is not a built-in sample dataset:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Missing MovieLens raw files are downloaded automatically; add --no_download when using existing files</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/generative/data/ml-1m/preprocess_ml_hstu.py</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># CPU smoke run; formal experiments normally use --device cuda and more epochs</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> outputs/hstu_ml</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/generative/run_hstu_movielens.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --device</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cpu</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --epoch</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --batch_size</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 16</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --save_dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> outputs/hstu_ml</span></span></code></pre></div><p>For Amazon Books, use:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/generative/data/amazon-books/preprocess_amazon_books.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --data_source</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bytedance</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkdir</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> outputs/hstu_amazon</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/generative/run_hstu_amazon_books.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --device</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cuda</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --epoch</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --batch_size</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 64</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --save_dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> outputs/hstu_amazon</span></span></code></pre></div><p>The preprocessors rewrite <code>vocab.pkl</code> and the three split files in the output directory. To preserve existing artifacts, back them up first or use <code>--output_dir</code> to write to a new directory. Amazon Books training generates full-vocabulary logits of shape <code>[B, L, V]</code>. The default script parameters assume approximately a 24 GB GPU; if memory is insufficient, reduce <code>--batch_size</code> and <code>--max_seq_len</code> first.</p><hr><h2 id="_2-hstulayer-eqs-2-4" tabindex="-1">2. HSTULayer: Eqs. 2-4 <a class="header-anchor" href="#_2-hstulayer-eqs-2-4" aria-label="Permalink to “2. HSTULayer: Eqs. 2-4”">​</a></h2><h3 id="_2-1-eq-2-joint-uvqk-projection" tabindex="-1">2.1 Eq. 2: Joint UVQK Projection <a class="header-anchor" href="#_2-1-eq-2-joint-uvqk-projection" aria-label="Permalink to “2.1 Eq. 2: Joint UVQK Projection”">​</a></h3><p><code>HSTULayer</code> first applies <code>LayerNorm</code> to the input, then uses one linear layer to generate <code>Q/K/U/V</code> together:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">proj_out </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> F.silu(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">self</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.proj1(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">self</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.norm_in(x)))</span></span></code></pre></div><p>Here, <code>SiLU</code> is applied to the complete <code>UVQK</code> projection before it is split. All four paths, <code>U</code>, <code>V</code>, <code>Q</code>, and <code>K</code>, therefore pass through the same nonlinearity, instead of activating only the gate path.</p><h3 id="_2-2-eq-3-adding-rab-p-t-to-attention-scores" tabindex="-1">2.2 Eq. 3: Adding <code>rab^{p,t}</code> to Attention Scores <a class="header-anchor" href="#_2-2-eq-3-adding-rab-p-t-to-attention-scores" aria-label="Permalink to “2.2 Eq. 3: Adding rab^{p,t} to Attention Scores”">​</a></h3><p>The current attention path is:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">scores </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (Q </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> K.transpose(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> /</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sqrt(dqk))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">scores </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> scores </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rab(time_diffs, seq_len)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">attn_weights </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> silu(scores) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> max_seq_len</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">AV</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> attn_weights </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> V</span></span></code></pre></div><p>Key points:</p><ul><li><code>rab^{p,t}</code> is a learnable per-head bias derived by bucketing relative position and relative time differences.</li><li>The bias is added to the attention scores, not to the input token embeddings.</li><li>HSTU uses <code>silu(scores) / N</code>, rather than standard Transformer softmax.</li><li>The causal mask prevents each position from attending to future tokens; the padding mask prevents PAD positions from being used as keys.</li></ul><p><code>RelativeBucketedTimeAndPositionBias</code> expects <code>time_diffs</code> to mean <code>query_time - timestamp[i]</code>. When pairwise differences are calculated, the anchor cancels out and yields the relative time differences between events.</p><h3 id="_2-3-eq-4-gated-output-and-projection" tabindex="-1">2.3 Eq. 4: Gated Output and Projection <a class="header-anchor" href="#_2-3-eq-4-gated-output-and-projection" aria-label="Permalink to “2.3 Eq. 4: Gated Output and Projection”">​</a></h3><p>The current output path is:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">gated </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> LayerNorm(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">AV</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> U</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">output </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> f2(gated)</span></span></code></pre></div><p>Notes:</p><ul><li><code>U</code> has already passed through <code>SiLU</code> in the joint projection from Eq. 2, so <code>SiLU(U)</code> is not applied again here.</li><li><code>proj2</code> receives only the gated attention output.</li><li><code>[U, x, gated]</code> is no longer concatenated, and there is no separate position-wise FFN.</li></ul><h3 id="_2-4-external-residual-connections" tabindex="-1">2.4 External Residual Connections <a class="header-anchor" href="#_2-4-external-residual-connections" aria-label="Permalink to “2.4 External Residual Connections”">​</a></h3><p><code>HSTUBlock</code> applies residual connections outside each layer:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> layer </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> self</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.layers:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> layer(x, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">padding_mask</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">padding_mask, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">time_diffs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">time_diffs)</span></span></code></pre></div><p>This avoids requiring <code>proj2</code> to learn an identity mapping and matches the inter-layer residual form in the HSTU paper/reference implementation.</p><hr><h2 id="_3-rab-p-t-and-time-features" tabindex="-1">3. <code>rab^{p,t}</code> and Time Features <a class="header-anchor" href="#_3-rab-p-t-and-time-features" aria-label="Permalink to “3. rab^{p,t} and Time Features”">​</a></h2><h3 id="_3-1-relativebucketedtimeandpositionbias" tabindex="-1">3.1 <code>RelativeBucketedTimeAndPositionBias</code> <a class="header-anchor" href="#_3-1-relativebucketedtimeandpositionbias" aria-label="Permalink to “3.1 RelativeBucketedTimeAndPositionBias”">​</a></h3><p><code>RelativeBucketedTimeAndPositionBias</code> maintains two sets of learnable parameters:</p><ul><li><code>pos_w</code>: per-head bias for the relative position difference <code>i - j</code>, with a table size of <code>2 * max_seq_len - 1</code>.</li><li><code>ts_w</code>: per-head bias for buckets of relative time differences, with a table size of <code>num_time_buckets + 1</code>.</li></ul><p>Time differences are bucketed as follows:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>dt = abs(time_diffs[i] - time_diffs[j]) / 60.0</span></span>
<span class="line"><span>bucket = sqrt(dt) or log(dt)</span></span>
<span class="line"><span>bucket = clamp(bucket / time_bucket_divisor, 0, num_time_buckets)</span></span></code></pre></div><p>When <code>time_diffs=None</code>, <code>rab</code> falls back to a position-only bias with shape <code>(1, H, L, L)</code>. When <code>time_diffs</code> is supplied, the output shape is <code>(B, H, L, L)</code>.</p><h3 id="_3-2-role-of-relposbias" tabindex="-1">3.2 Role of <code>RelPosBias</code> <a class="header-anchor" href="#_3-2-role-of-relposbias" aria-label="Permalink to “3.2 Role of RelPosBias”">​</a></h3><p><code>RelPosBias</code> remains in <code>hstu_utils.py</code>, but it is a legacy relative-position bias:</p><ul><li>HLLM still reuses it.</li><li>It can be used independently for compatibility with older experiments.</li><li>The current HSTU main path no longer uses it. Relative position/time modeling on HSTU attention scores is handled by <code>RelativeBucketedTimeAndPositionBias</code>.</li></ul><hr><h2 id="_4-hstumodel-wrapper" tabindex="-1">4. HSTUModel Wrapper <a class="header-anchor" href="#_4-hstumodel-wrapper" aria-label="Permalink to “4. HSTUModel Wrapper”">​</a></h2><p><code>HSTUModel</code> converts item token sequences into hidden states, invokes <code>HSTUBlock</code>, and outputs vocabulary logits.</p><p>The current wrapper contains:</p><ul><li><code>token_embedding(vocab_size, d_model, padding_idx=0)</code>.</li><li>Absolute <code>position_embedding(max_seq_len, d_model)</code>.</li><li>Optional input-side <code>time_embedding(num_time_buckets, d_model)</code>, controlled by <code>use_time_embedding</code>.</li><li><code>HSTUBlock(..., num_time_buckets, time_bucket_fn, time_bucket_divisor)</code>.</li><li>A tied-embedding output projection by default: <code>F.linear(hstu_output, token_embedding.weight, output_bias)</code>.</li></ul><p>Two time/position paths must be distinguished:</p><ul><li>Relative position/time modeling for core HSTU Eq. 3 occurs on the attention scores through <code>rab^{p,t}</code>.</li><li>The current <code>HSTUModel</code> wrapper retains absolute position embeddings and optional input-side time embeddings for compatibility with the existing data interface and experiment settings.</li></ul><p>PAD token is defined as <code>0</code>. The model explicitly zeroes PAD rows after input embedding and after HSTU output, preventing position/time embeddings from leaking signals through PAD positions.</p><hr><h2 id="_5-data-and-training-conventions" tabindex="-1">5. Data and Training Conventions <a class="header-anchor" href="#_5-data-and-training-conventions" aria-label="Permalink to “5. Data and Training Conventions”">​</a></h2><h3 id="_5-1-time-difference-semantics" tabindex="-1">5.1 Time-Difference Semantics <a class="header-anchor" href="#_5-1-time-difference-semantics" aria-label="Permalink to “5.1 Time-Difference Semantics”">​</a></h3><p>The preprocessing scripts generate <code>seq_time_diffs</code> with the following semantics:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>time_diffs[i] = query_time - timestamp[i]</span></span></code></pre></div><p>Here, <code>query_time</code> is typically the timestamp of the last behavior in the current history. For example:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>timestamps  = [100, 200, 300, 400]</span></span>
<span class="line"><span>query_time  = 400</span></span>
<span class="line"><span>time_diffs  = [300, 200, 100, 0]</span></span></code></pre></div><p>This format allows <code>rab</code> to recover the relative time differences between events through pairwise subtraction.</p><h3 id="_5-2-dataset-format" tabindex="-1">5.2 Dataset Format <a class="header-anchor" href="#_5-2-dataset-format" aria-label="Permalink to “5.2 Dataset Format”">​</a></h3><p><code>SequenceDataGenerator</code> uses four-tuples:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>(seq_tokens, seq_positions, seq_time_diffs, targets)</span></span></code></pre></div><p><code>seq_positions</code> is retained for data-interface compatibility. The current <code>HSTUModel</code> generates position indices internally from the sequence length during <code>forward</code>, and <code>SeqTrainer</code> does not use the batch&#39;s <code>seq_positions</code>.</p><h3 id="_5-3-training-objective" tabindex="-1">5.3 Training Objective <a class="header-anchor" href="#_5-3-training-objective" aria-label="Permalink to “5.3 Training Objective”">​</a></h3><p><code>SeqTrainer</code> uses full-sequence next-token cross-entropy:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>logits[:, i, :] -&gt; seq_tokens[:, i + 1]</span></span>
<span class="line"><span>logits[:, -1, :] -&gt; targets</span></span></code></pre></div><p>PAD token <code>0</code> is excluded from the loss through <code>ignore_index=0</code>. During validation/testing, <code>evaluate</code> returns the average loss and top-1 accuracy for the final held-out target. Ranking evaluation in the example scripts additionally computes HR@K and NDCG@K.</p><hr><h2 id="_6-alignment-and-differences-relative-to-meta-s-reference" tabindex="-1">6. Alignment and Differences Relative to Meta&#39;s Reference <a class="header-anchor" href="#_6-alignment-and-differences-relative-to-meta-s-reference" aria-label="Permalink to “6. Alignment and Differences Relative to Meta&#39;s Reference”">​</a></h2><p>Main points of alignment:</p><ul><li>Eq. 2: the complete <code>UVQK</code> projection passes through <code>SiLU</code> before splitting.</li><li>Eq. 3: per-head <code>rab^{p,t}</code> is added to attention scores, followed by <code>silu(scores) / N</code>.</li><li>Eq. 4: <code>f_2(LayerNorm(AV) * U)</code>, with no concat-u/x bypass and no additional FFN.</li><li>Inter-layer residuals: applied outside each layer by <code>HSTUBlock</code>.</li><li>Time differences: the recommended form is the anchor-delta representation <code>query_time - timestamp[i]</code>.</li></ul><p>Remaining engineering differences:</p><ul><li>DLRM, multi-task heads, and complex feature crossing are not included; this implementation focuses on single-task next-item prediction.</li><li>The <code>HSTUModel</code> wrapper retains absolute position embeddings and optional input-side time embeddings; the core HSTU layer injects relative position/time bias through <code>rab^{p,t}</code>.</li><li>Multi-step autoregressive decoding and generation interfaces such as beam search are not wrapped.</li><li>Initialization and some training settings do not target bit-level reproduction.</li></ul><hr><h2 id="_7-summary-of-recent-hstu-changes" tabindex="-1">7. Summary of Recent HSTU Changes <a class="header-anchor" href="#_7-summary-of-recent-hstu-changes" aria-label="Permalink to “7. Summary of Recent HSTU Changes”">​</a></h2><ul><li>Corrected Eq. 2: <code>SiLU</code> now applies to the complete <code>UVQK</code> projection instead of only the gate path.</li><li>Corrected Eq. 3: added <code>RelativeBucketedTimeAndPositionBias</code> and connected <code>rab^{p,t}</code> to the attention scores.</li><li>Corrected Eq. 4: removed the <code>[U, x, gated]</code> concatenation bypass, and made <code>proj2</code> project only the gated attention output.</li><li>Corrected the inter-layer structure: <code>HSTUBlock</code> now uses the external residual form <code>x = x + layer(x)</code>.</li><li>Updated documentation and API notes: clarified that <code>RelPosBias</code> belongs to the legacy/HLLM path, while the HSTU main path uses <code>rab^{p,t}</code>.</li></ul>`,77)])])}const u=i(n,[["render",o]]);export{k as __pageData,u as default};
