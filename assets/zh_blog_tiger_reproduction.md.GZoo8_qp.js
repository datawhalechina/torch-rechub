import{_ as i,c as s,o as a,aj as t}from"./chunks/framework.DJhf6x5b.js";const k=JSON.parse('{"title":"TIGER 复现说明","description":"TIGER 生成式推荐模型在 torch-rechub 中的运行说明，包括语义 ID 流水线、运行模式与 toy 训练流程","frontmatter":{"title":"TIGER 复现说明","description":"TIGER 生成式推荐模型在 torch-rechub 中的运行说明，包括语义 ID 流水线、运行模式与 toy 训练流程"},"headers":[],"relativePath":"zh/blog/tiger_reproduction.md","filePath":"zh/blog/tiger_reproduction.md"}'),n={name:"zh/blog/tiger_reproduction.md"};function o(d,e,l,h,r,c){return a(),s("div",null,[...e[0]||(e[0]=[t(`<h2 id="tiger-在-torch-rechub-中的复现说明" tabindex="-1">TIGER 在 torch-rechub 中的复现说明 <a class="header-anchor" href="#tiger-在-torch-rechub-中的复现说明" aria-label="Permalink to “TIGER 在 torch-rechub 中的复现说明”">​</a></h2><p>本文件说明当前 <code>torch-rechub</code> 中 TIGER（Transformer Index for GEnerative Recommenders）的实现与运行方式。TIGER 把&quot;预测下一个 item&quot;建模成&quot;生成下一个 item 的语义 ID&quot;的序列到序列任务：每个 item 先由 RQ-VAE 量化成一串 codebook token（语义 ID，如 <code>&lt;a_1&gt;&lt;b_3&gt;&lt;c_5&gt;</code>），再用 T5 自回归地生成下一个 item 的语义 ID，并通过前缀受限的 beam search 约束到合法 item 上。</p><p>示例脚本按数据集拆分，与 HSTU / HLLM 保持一致的&quot;一数据集一脚本&quot;风格：</p><ul><li><code>examples/generative/run_tiger_movielens.py</code></li><li><code>examples/generative/run_tiger_amazon_books.py</code></li></ul><hr><h2 id="_1-模块划分" tabindex="-1">1. 模块划分 <a class="header-anchor" href="#_1-模块划分" aria-label="Permalink to “1. 模块划分”">​</a></h2><ul><li><strong>模型主体</strong>：<code>torch_rechub/models/generative/tiger.py</code><ul><li><code>TIGERModel</code>：继承自 <code>transformers.T5ForConditionalGeneration</code>，新增 <code>set_hyper(temperature)</code> 与温度缩放的 <code>ranking_loss</code>。</li></ul></li><li><strong>数据与受限解码</strong>：<code>torch_rechub/utils/data.py</code><ul><li><code>TigerSeqDataset</code>：把 <code>inter.json</code> 中的 item id 序列映射成语义 ID 字符串，并按 <code>train</code> / <code>valid</code> / <code>test</code> 三种模式做 leave-one-out 切分。</li><li><code>Trie</code>：根据所有合法语义 ID 构建前缀树，生成 <code>prefix_allowed_tokens_fn</code> 用于受限 beam search。</li></ul></li><li><strong>语义 ID 生成</strong>：<code>examples/generative/run_rqvae_amazon_books.py</code><ul><li>用 RQ-VAE 对 item embedding 做残差量化，导出 <code>semantic_ids.json</code>。</li></ul></li><li><strong>示例脚本</strong>：<code>run_tiger_movielens.py</code> / <code>run_tiger_amazon_books.py</code>，各自实现 <code>train</code> / <code>test</code> 与 toy 数据生成。</li></ul><hr><h2 id="_2-数据格式" tabindex="-1">2. 数据格式 <a class="header-anchor" href="#_2-数据格式" aria-label="Permalink to “2. 数据格式”">​</a></h2><p>TIGER 需要两个 JSON 文件：</p><ul><li><strong><code>inter.json</code></strong>：<code>{user_id: [item_id, item_id, ...]}</code>，每个用户按时间排序的 item id 历史。item id 从 1 开始，<code>0</code> 保留给 padding。</li><li><strong><code>semantic_ids.json</code></strong>：<code>{item_id: [&quot;&lt;a_..&gt;&quot;, &quot;&lt;b_..&gt;&quot;, ...]}</code>，<code>inter.json</code> 中出现的每个 item id 都要有对应的 RQ-VAE 语义 ID。</li></ul><p><code>TigerSeqDataset</code> 的切分约定（leave-one-out）：</p><ul><li><code>train</code>：对历史 <code>items[:-2]</code> 逐位置展开成多个 <code>(history, next_item)</code> 样本。</li><li><code>valid</code>：以 <code>items[:-2]</code> 为历史，<code>items[-2]</code> 为标签。</li><li><code>test</code>：以 <code>items[:-1]</code> 为历史，<code>items[-1]</code> 为标签。</li></ul><p>因此每个用户至少需要 3 个交互才能产生训练样本。</p><hr><h2 id="_3-运行模式" tabindex="-1">3. 运行模式 <a class="header-anchor" href="#_3-运行模式" aria-label="Permalink to “3. 运行模式”">​</a></h2><p>两个脚本都用 <code>--mode</code> 控制运行阶段：</p><table tabindex="0"><thead><tr><th>mode</th><th>说明</th></tr></thead><tbody><tr><td><code>generate-toy-data</code></td><td>生成一份小规模合成数据，写到 <code>--data_inter_path</code> / <code>--data_indice_path</code>（与读取路径完全一致）</td></tr><tr><td><code>prepare-data</code></td><td>仅 MovieLens：从真实 <code>ratings.dat</code> 构建 <code>inter.json</code> 和 <code>movie_id_map.json</code></td></tr><tr><td><code>train</code></td><td>加入语义 ID token、<code>resize_token_embeddings</code>、从头训练 T5（随机初始化，不加载预训练权重），保存 tokenizer / config / 模型到 <code>--output_dir</code></td></tr><tr><td><code>test</code></td><td>从 <code>--ckpt_path</code>（默认回退到 <code>--output_dir</code>）加载，做受限 beam search 并报告 hit@k / ndcg@k</td></tr><tr><td><code>all</code></td><td>依次执行 <code>generate-toy-data</code> → <code>train</code> → <code>test</code>（默认）</td></tr></tbody></table><p>关键实现细节：</p><ul><li><strong>从头训练，不加载预训练权重</strong>：TIGER 论文中 T5 编码器-解码器是随机初始化、从头训练的（语义 ID 词表不是自然语言，预训练的 NL 权重无意义）。<code>train()</code> 用 <code>TIGERModel(config)</code> 从 <code>--base_model</code> 的 config 构建架构并随机初始化，<code>--base_model</code> 只提供架构/config 与 tokenizer，不加载预训练权重。</li><li><strong>训练前必须加入语义 ID token</strong>：<code>train()</code> 会 <code>tokenizer.add_tokens(dataset.get_new_tokens())</code> 后再 <code>resize_token_embeddings</code>，否则 <code>&lt;a_1&gt;</code> 这类 token 会被 T5 切成子词，训练无意义。</li><li><strong>生成与读取路径一致</strong>：<code>generate-toy-data</code> 直接写到数据集读取的同一路径，避免&quot;生成文件名 ≠ 读取文件名&quot;。</li><li><strong>test 从 checkpoint 加载词表与权重</strong>：用 checkpoint 自身的 config 加载模型（避免把扩词表后的 <code>vocab_size</code> 传进 <code>from_pretrained</code> 触发 embedding 尺寸不匹配），再按 tokenizer 对齐；若 checkpoint 缺少某些语义 ID token，会自动补齐并 resize 后再评估。</li></ul><hr><h2 id="_4-toy-快速跑通" tabindex="-1">4. Toy 快速跑通 <a class="header-anchor" href="#_4-toy-快速跑通" aria-label="Permalink to “4. Toy 快速跑通”">​</a></h2><p>无需任何外部数据，CPU 即可端到端验证：</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/generative</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># MovieLens 形态的合成数据</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run_tiger_movielens.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --mode</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> all</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --toy_num_users</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 16</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --toy_num_items</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 20</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --epochs</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --per_device_batch_size</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --num_beams</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --test_batch_size</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --num_workers</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Amazon-Books 的内置 toy 数据</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run_tiger_amazon_books.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --mode</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> all</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --epochs</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 5</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --per_device_batch_size</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --num_beams</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --test_batch_size</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --num_workers</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span></span></code></pre></div><p>正常会看到 <code>Added N semantic-id tokens</code> → <code>Model saved to ...</code> → <code>Test results: {...}</code> 的输出。</p><blockquote><p>离线/无法访问 HuggingFace Hub 时，<code>t5-small</code> 这个旧别名可能无法解析，请改用规范仓库名：<code>--base_model google-t5/t5-small</code>。</p></blockquote><hr><h2 id="_5-真实数据流程-movielens-1m" tabindex="-1">5. 真实数据流程（MovieLens-1M） <a class="header-anchor" href="#_5-真实数据流程-movielens-1m" aria-label="Permalink to “5. 真实数据流程（MovieLens-1M）”">​</a></h2><p>真实数据需要先得到与 <code>inter.json</code> 对齐的语义 ID，整体是 RQ-VAE → TIGER 两段式流水线：</p><ol><li><p><strong>构建交互序列</strong>：</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run_tiger_movielens.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --mode</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> prepare-data</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --ratings_path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./data/ml-1m/ratings.dat</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --data_inter_path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./data/ml-1m/tiger/inter.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --min_seq_len</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 5</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --max_his_len</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 20</span></span></code></pre></div><p>这一步按时间戳排序、过滤交互过少的用户，把 movie id 重映射成连续的 1-based item id，并额外写出 <code>movie_id_map.json</code>。</p></li><li><p><strong>生成语义 ID</strong>：为同一批 item id 准备 item embedding（例如 HLLM 预处理产出的文本/ID embedding），用 <code>run_rqvae_amazon_books.py</code> 风格的 RQ-VAE 训练并导出 <code>semantic_ids.json</code>。<strong>RQ-VAE 输出必须按第 1 步相同的 item id 作为 key</strong>，否则 <code>inter.json</code> 与 <code>semantic_ids.json</code> 对不上。</p></li><li><p><strong>训练与测试</strong>：</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run_tiger_movielens.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --mode</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> train</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --data_inter_path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./data/ml-1m/tiger/inter.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --data_indice_path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./data/ml-1m/tiger/semantic_ids.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --output_dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./ckpt/tiger_ml</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run_tiger_movielens.py</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --mode</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ckpt_path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./ckpt/tiger_ml</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --data_inter_path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./data/ml-1m/tiger/inter.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    --data_indice_path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./data/ml-1m/tiger/semantic_ids.json</span></span></code></pre></div></li></ol><p>Amazon-Books 的真实数据流程相同：先用 <code>run_rqvae_amazon_books.py</code> 得到 <code>semantic_ids.json</code>，再准备 <code>inter.json</code>，最后 <code>--mode train</code> / <code>--mode test</code>。</p><hr><h2 id="_6-评估" tabindex="-1">6. 评估 <a class="header-anchor" href="#_6-评估" aria-label="Permalink to “6. 评估”">​</a></h2><p><code>test</code> 阶段对每个测试样本做受限 beam search：</p><ul><li>用 <code>Trie</code> 构建 <code>prefix_allowed_tokens_fn</code>，使每一步只允许生成语义 ID 表中合法的下一个 token。</li><li><code>--num_beams</code> 同时作为 <code>num_return_sequences</code>，得到 top-N 候选；<code>--filter_items</code> 会把不在合法 item 集合里的生成结果打到极低分。</li><li>指标为 leave-one-out 的 <code>hit@k</code> 与 <code>ndcg@k</code>（每个用户只有一个 ground-truth，因此 IDCG=1）。通过 <code>--metrics hit@1,hit@5,hit@10,ndcg@5,ndcg@10</code> 配置。</li></ul><hr><h2 id="_7-常见问题" tabindex="-1">7. 常见问题 <a class="header-anchor" href="#_7-常见问题" aria-label="Permalink to “7. 常见问题”">​</a></h2><ul><li><strong><code>OSError: We couldn&#39;t connect to &#39;https://huggingface.co&#39;</code></strong>：离线环境用 <code>--base_model google-t5/t5-small</code>（规范仓库名），并确保本地已缓存对应权重。</li><li><strong><code>Trainer.__init__() got an unexpected keyword argument &#39;tokenizer&#39;</code></strong>：<code>transformers&gt;=5</code> 把 <code>tokenizer</code> 参数改名为 <code>processing_class</code>，脚本已按签名自动适配，无需手动改。</li><li><strong><code>&#39;TIGERModel&#39; object has no attribute &#39;model_parallel&#39;</code></strong>：旧版 T5 的 model-parallel 守卫在 <code>transformers&gt;=5</code> 不再初始化，<code>TIGERModel.__init__</code> 已补 <code>model_parallel=False</code> / <code>device_map=None</code>，多卡 DataParallel 也能跑。</li><li><strong>生成结果总是子词、命中率异常低</strong>：确认训练时确实执行了 <code>add_tokens</code> + <code>resize_token_embeddings</code>（日志里有 <code>Added N semantic-id tokens</code>），且 <code>test</code> 是从训练保存的 <code>--output_dir</code> 加载词表。</li></ul>`,38)])])}const g=i(n,[["render",o]]);export{k as __pageData,g as default};
